# Jupyter notebooks

按编号顺序运行 Notebook。不要提交其输出、检查点或由 Notebook 生成的模型、图表和表格；这些内容已由 `.gitignore` 排除。

- `01_exploratory_analysis.ipynb`：数据质量与探索性分析。
- `02_model_training_evaluation.ipynb`：时间外模型训练、评估与解释。

共享逻辑放在 `src/road_severity/`，避免在多个 Notebook 中复制转换或训练代码。
